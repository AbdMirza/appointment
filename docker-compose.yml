
# services:
#   backend:
#     build: ./backend
#     ports:
#       - "5000:5000"
#     environment:
#       - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/appointment_db?schema=public
#       - JWT_SECRET=supersecretkey
#       - PORT=5000
#       - RESEND_API_KEY=${RESEND_API_KEY}
#       - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
#       - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
#       - REDIS_URL=redis://redis:6379
#       - FRONTEND_URL=${FRONTEND_URL}
#       - EMAIL_FROM=${EMAIL_FROM}
#       - EMAIL_FROM_NAME=${EMAIL_FROM_NAME}
#     command: sh -c "npx prisma migrate deploy && npm start"
#     volumes:
#       - ./backend/public/invoices:/app/public/invoices
#     depends_on:
#       - postgres
#       - redis

#   studio:
#     build: ./backend
#     command: npx prisma studio --browser none --hostname 0.0.0.0
#     ports:
#       - "5555:5555"
#     environment:
#       - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/appointment_db?schema=public
#     depends_on:
#       - postgres

#   redis:
#     image: redis:alpine
#     ports:
#       - "6379:6379"

#   postgres:
#     image: postgres:15
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: appointment_db
#     ports:
#       - "5432:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:


services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=supersecretkey
      - PORT=5000
      - RESEND_API_KEY=${RESEND_API_KEY}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - REDIS_URL=redis://redis:6379
      - FRONTEND_URL=${FRONTEND_URL}
      - EMAIL_FROM=${EMAIL_FROM}
      - EMAIL_FROM_NAME=${EMAIL_FROM_NAME}
    command: sh -c "npx prisma migrate deploy && npm start"
    volumes:
      - ./backend/public/invoices:/app/public/invoices
    depends_on:
      - redis

  studio:
    build: ./backend
    command: npx prisma studio --browser none --hostname 0.0.0.0
    ports:
      - "5555:5555"
    environment:
      - DATABASE_URL=${DATABASE_URL}

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"